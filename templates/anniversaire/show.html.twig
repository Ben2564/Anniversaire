{% extends 'base.html.twig' %}

{% block title %}Anniversaire{% endblock %}

{% block body %}
    <h1>Anniversaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ anniversaire.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ anniversaire.nom }}</td>
            </tr>
            <tr>
                <th>DateAnniv</th>
                <td>{{ anniversaire.dateAnniv ? anniversaire.dateAnniv|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_anniversaire_index') }}">back to list</a>

    <a href="{{ path('app_anniversaire_edit', {'id': anniversaire.id}) }}">edit</a>

    {{ include('anniversaire/_delete_form.html.twig') }}
{% endblock %}
