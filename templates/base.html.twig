<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Bonjour{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
        <style>
            .navbar {
                list-style-type: none;
                margin: 10;
                padding: 5;
                overflow: hidden;
                background-color: #333;
            }

            .navbar li {
                float: left;
            }

            .navbar .logo {
                padding: 15px;
                color: white;
            }

            .nav-right {
                float: right;
            }

            .navbar a {
                display: block;
                color: white;
                text-align: center;
                padding: 14px 16px;
                text-decoration: none;
            }

            .navbar a:hover {
                background-color: #ddd;
                color: black;
            }

            .button {
                display: inline-block;
                padding: 10px 20px;
                font-size: 16px;
                text-align: center;
                text-decoration: none;
                border: 2px solid grey;
                border-radius: 5px;
                color: black;
                background-color: grey;
            }

            .button2 {
                display: inline-block;
                padding: 6px;
                text-align: center;
                text-decoration: none;
                border: 2px solid grey;
                border-radius: 5px;
                color: black;
            }

            .button-container {
                display: flex;
                align-items: center;
            }

            .body {
                display: flex;
                flex-direction: column;
                justify-content: center;
                align-items: center;
                margin: 0;
            }

            .form {
                display: flex;
                flex-direction: column;
                justify-content: center;
                align-items: center;
                margin: 0;
            }
            .esp {
                padding: 5px;
            }
        </style>
    </head>
    <body>
        <nav>
            <ul class="navbar">
                <li class="logo">Logo</li>
                <li class="nav-right"><a href="{{ path('creer_Compte') }}">Créer un compte</a></li>
                <li class="nav-right"><a href="{{ path('se_Connecter') }}">Se connecter</a></li>
            </ul>
        </nav>
        <div class="body">
            {% block body %}{% endblock %}
        </div>
    </body>
</html>
